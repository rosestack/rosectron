type Mode = "development" | "preview" | "production";

type Process = "main" | "preload" | "renderer";

const mode: Mode = null as any;

const process: Process = null as any;

export {
  mode,
  process,
};